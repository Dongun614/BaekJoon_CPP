#include <iostream>

using namespace std;

int main(){
    int slot, turn;
    int start, space;

    cin >> slot >> turn;

    int* row = (int*)malloc(sizeof(int)*slot);
    memset(row, 0, sizeof(int) * slot);

    for(int i=0; i<turn; i++){
        cin >> 
    }

    return 0;
}